package model;

public class Entrepot extends Building {
    public Entrepot(String type, String icon, Input input, Output output, int productionInterval) {
        super(type, icon, input, output, productionInterval);
    }
}
