package model;

public class Usine extends Building {
    public Usine(String type, String icon, Input input, Output output, int productionInterval) {
        super(type, icon, input, output, productionInterval);
    }
}
